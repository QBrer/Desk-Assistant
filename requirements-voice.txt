# Runtime pins for the local desktop voice services.
# ctranslate2 4.7.x can crash on this Windows/Anaconda setup when faster-whisper
# instantiates WhisperModel, so keep 4.6.x unless the native runtime is retested.
faster-whisper>=1.2,<1.3
ctranslate2==4.6.0
numpy>=1.26,<2
setuptools>=70,<71
